# SafeRoute
SafeRoute is a smart web application that predicts accident risk in real time using live data like weather, traffic, and route conditions. It calculates accurate routes and distances using map APIs, ensuring reliable navigation insights. The system uses a lightweight machine learning model to dynamically assess risk levels along the route.
